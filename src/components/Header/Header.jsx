import logo from '../../assets/logo.svg';

const Header = () => (
  <header className="App-header">
    <img src={logo} className="App-logo" alt="logo" />
    Hacker News (Reloaded)
  </header>
);

export default Header;
